{
  "name": "ProyectoParcial1",
  "version": "1.0.0",
  "description": "POS simple para Tienda UPB (HTML/CSS/JS + Node, datos en JSON)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true
}
